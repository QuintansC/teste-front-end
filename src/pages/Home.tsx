import Form from '../components/Form';
import { Container } from '@material-ui/core';
const App = ()=>{
  return (
    <Container className="App">
      <Form onSubmit={()=>{}}/>
    </Container>
  );
}

export default App;
