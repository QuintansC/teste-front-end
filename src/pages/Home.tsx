import Form from '../components/Form';
import { Container } from '@mui/material';
const App = ()=>{
  return (
    <Container className="App">
      <Form onSubmit={()=>{}}/>
    </Container>
  );
}

export default App;
